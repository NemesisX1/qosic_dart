/// Enum to handle the different network available on Qosic
enum QosicNetwork {
  /// for mtn
  mtn,

  /// for moov
  moov,
}

// Basic enum to handle qosic contries
enum QosicCountry {
  benin,
  togo,
}

// enum QosicStatus {
//   SUCCESSFUL,
//   PENDING,
//   FAILED,
// }
